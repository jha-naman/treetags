[package]
name = "treetags-plugin-terraform"
version = "0.1.0"
edition = "2021"
description = "Terraform plugin for treetags"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
treetags-plugin-common = { path = "../common", features = ["tree-walker"] }
tree-sitter = "0.26.5"
tree-sitter-hcl = "1.1.0"
wit-bindgen = "0.58"
